<div class="col-sm-12 col-xs-12">
  <h2>JSON Documents</h2>
  
  <%= link_to 'New JSON Document', new_record_path, class: 'btn btn-primary' %>
  
  <% if @records.any? %>
    <table class='table'>
      <%= @records.each do |record| %>
        <tr><td><%= link_to record.id, record %></td></tr>
      <% end %>
    </table>
  <% end %>
  
</div>
